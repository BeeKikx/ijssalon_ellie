# Les 10.1.3 Maak functie som(); tel values op
def som(input):
    #output = 0
    #for key, value in input.items():               '''Deze code gaf alleen de 1e key aan
        #output += value                            '''in de hoofdcode ook geen andere values die niet terugkwamen
        #return output
    return sum(input.values())
